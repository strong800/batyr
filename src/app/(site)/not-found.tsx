import { NotFoundContent } from '@/components/layout/NotFoundContent';

/** 404 для случаев, когда notFound() вызвала страница проекта или объекта. */
export default function NotFound() {
  return <NotFoundContent />;
}
